import { NextRequest, NextResponse } from "next/server";
export declare function handleAuthenticationInRequest(request: NextRequest, verbose: boolean, cookieConfig: {
    maxAge: number | null;
}): Promise<{
    kind: "redirect";
    response: NextResponse;
} | {
    kind: "refreshTokens";
    refreshTokens: {
        token: string;
        refreshToken: string;
    } | null | undefined;
}>;
//# sourceMappingURL=request.d.ts.map